#include<bits/stdc++.h>
using namespace  std;

int main(){
    system("CLS");

    // char array
    char ch[5]={'a','b','c','d','e'};

    // printing and accesing the array
    cout<<"value at 3 index "<<ch[3]<<endl;
    for(int i=0;i<5;i++){
        cout<<ch[i]<<endl;
    }

    // double, float and bool array  array
    double firstDouble[5];
    float firstFloat[6];
    bool firstBool[9];

    return 0;
}